name: api
version: 0.0.1
endpoints:
  profile:
    file: profile.js
    response:
      success:
        description: Success
        example: |
          {
            "username": "john"
          }
      fail:
        exit_code: 400
        description: Failed
        example: |
          {
            "message": "User was not found."
          }
