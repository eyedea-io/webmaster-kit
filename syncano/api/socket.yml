name: api
version: 0.0.1

classes:
  user:
    - name: firstName
      type: string
    - name: lastName
      type: string

endpoints:
  user/profile:
    file: user/profile.js
    response:
      success:
        exit_code: 200
      fail:
        exit_code: 400
